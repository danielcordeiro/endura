# Forca instalar devDependencies mesmo com NODE_ENV=production setado.
# Necessario porque o build precisa de typescript, @types/*, drizzle-kit, tsx.
# Sem isso, pnpm 9+ pula devDeps quando NODE_ENV=production e o tsc falha.
production=false
